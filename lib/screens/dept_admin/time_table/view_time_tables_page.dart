import 'package:flutter/material.dart';

class ViewTimeTablesPage extends StatelessWidget {
  final String orgId;
  final String deptId;

  const ViewTimeTablesPage({
    super.key,
    required this.orgId,
    required this.deptId,
  });

  @override
  Widget build(BuildContext context) {
    return const SizedBox.expand();
  }
}
